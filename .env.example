# =============================
# Environment Configuration
# =============================
NODE_ENV=development        # The environment mode (development, production, local, dev, prod). Default: development
VERTICAL=                   # The vertical of the AWS Lambda Function (Rides, Delivery). Required.

# =============================
# Finance API Configuration
# =============================
FINANCE_BASE_URL=           # Finance API base URL. Must be a valid URL. Required.
FINANCE_API_KEY=            # Finance API key for authentication. Required.
FINANCE_DISPERSION_ENDPOINT=/api/v1/dispersion/receive  # Finance API endpoint for uploading dispersion files. Default: /api/v1/dispersion/receive

# =============================
# AWS Configuration
# =============================
AWS_ACCESS_KEY_ID=          # Your AWS Access Key ID. Optional (can use IAM roles).
AWS_SECRET_ACCESS_KEY=      # Your AWS Secret Access Key. Optional (can use IAM roles).
AWS_REGION=                 # The AWS region (e.g., us-east-1). Required.
S3_BUCKET_NAME=             # The S3 bucket name for file storage. Required.
ALLOWED_S3_BUCKETS=         # Comma-separated S3 bucket names from which workflows trigger (e.g. my-bucket-dev,my-bucket-prod). Required.

# =============================
# Application Configuration
# =============================
AWS_APP_NAME=               # The AWS application name identifier. Required.
EXPIRATION_HOURS=12         # The number of hours after which a file is considered expired. Default: 12

# =============================
# PostgreSQL Configuration
# =============================
PG_HOST=                    # The hostname or IP address of the PostgreSQL server. Required.
PG_PORT=5432                # The port number for PostgreSQL (1-65535). Default: 5432
PG_DATABASE=                # The name of the PostgreSQL database. Required.
PG_USER=                    # The username for PostgreSQL authentication. Required.
PG_PASSWORD=                # The password for PostgreSQL authentication. Required.
PG_SSL=false                # Whether to use SSL for PostgreSQL connection (true/false). Default: false
PG_MAX_CONNECTIONS=10       # Maximum number of connections in the pool (min: 1). Default: 10
PG_CONNECTION_TIMEOUT=30000 # Connection timeout in milliseconds (min: 1000). Default: 30000
PG_IDLE_TIMEOUT=30000       # Idle timeout in milliseconds (min: 1000). Default: 30000

# =============================
# MongoDB Configuration
# =============================
MONGO_URI=                  # The connection URI for MongoDB (e.g., mongodb://localhost:27017). Required.
MONGO_DATABASE=             # The name of the MongoDB database. Required.
MONGO_MAX_POOL_SIZE=10      # Maximum number of connections in the pool (min: 1). Default: 10
MONGO_MIN_POOL_SIZE=1       # Minimum number of connections in the pool (min: 0). Default: 1
MONGO_MAX_IDLE_TIME=30000   # Maximum idle time for connections in milliseconds (min: 1000). Default: 30000
MONGO_SERVER_SELECTION_TIMEOUT=30000 # Server selection timeout in milliseconds (min: 1000). Default: 30000

# =============================
# N8N Configuration
# =============================
N8N_HOST=                   # The N8N server URL. Must be a valid URL. Required.
N8N_API_KEY=                # The API key for N8N server authentication. Required.